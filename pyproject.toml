[project]
name = "mae5110"
version = "0.1.0"
description = "Course exercises for MAE 5110"
readme = "README.md"
requires-python = ">=3.14"
dependencies = [
    "matplotlib",
    "numpy",
    "pillow>=12.3.0",
    "ruff",
]

[dependency-groups]
dev = [
    "pytest",
]
